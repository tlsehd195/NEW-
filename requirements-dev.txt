# Full development/test environment freeze, captured from the venv this
# project's full pytest suite was actually run against (session 2026-09-24).
# Corrected 2026-09-24: an earlier version of this file's comment claimed
# scipy/seaborn/statsmodels/yfinance/requests/beautifulsoup4/lxml/curl_cffi
# etc. were undeclared direct dependencies. Checked directly (`pip show
# <pkg>` Required-by): every one of them is a real transitive dependency of
# `quantstats` (`reporting` extra), `alphalens-reloaded` (`research` extra),
# or `alpaca-py` (`broker-verification` extra) -- already covered by
# `pyproject.toml`, nothing missing there. `scrapling` (plus the four
# packages only it needed -- cssselect/orjson/w3lib/tld) was the one
# genuine outlier: `pip show scrapling` had an empty Required-by (nothing
# else pulls it in) and it appears nowhere in src/, scripts/, tests/, or
# any doc/ADR in this repo -- dead weight from some past exploration,
# removed here. Full suite re-run without it: still green.
#
# Installed by .claude/hooks/session-start.sh on every Claude Code web
# session start. Regenerate with:
#   pip freeze | grep -v -E "^(aiinvest==|-e git|pip==|setuptools==|wheel==)"
#
# 2026-09-26 addition (ADR-0207): exchange_calendars (`market-calendars`
# extra) is now imported at module level by
# data_infra.exchange_calendars_adapter, which
# scripts/run_paper_trading_cycle.py / run_multi_strategy_paper_trading_cycle.py /
# run_long_horizon_validation.py import unconditionally -- tests that load
# those scripts as modules (e.g. tests/scripts/test_run_monitoring_sweep.py)
# need it installed too, not just present as an optional extra a caller
# might skip. korean_lunar_calendar/pyluach/toolz are its own transitive
# dependencies (confirmed via `pip show exchange_calendars` Required-by
# chain, same verification discipline as this file's other entries).
#
# 2026-09-26 addition (ADR-0207): purgedcv (`backtest-integrity-stats`
# extra) is imported at module level by
# scripts/cross_verify_pbo_dsr_with_purgedcv.py (scripts/-only, src/
# unmodified). cloudpickle/joblib/scikit-learn/threadpoolctl are its own
# transitive dependencies.
alpaca-py==0.44.0
alphalens-reloaded==0.4.6
annotated-types==0.8.0
asttokens==3.0.2
beautifulsoup4==4.15.0
Bottleneck==1.6.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cloudpickle==3.1.2
contourpy==1.3.3
curl_cffi==0.16.3
cycler==0.12.1
duckdb==1.5.5
empyrical-reloaded==0.5.12
exchange_calendars==4.13.2
executing==2.2.1
fonttools==4.65.0
formulaic==1.2.2
idna==3.20
iniconfig==2.3.0
interface_meta==2.0.1
ipython==9.17.1
ipython_pygments_lexers==1.1.1
jedi==0.20.0
joblib==1.6.0
kiwisolver==1.5.1
korean_lunar_calendar==0.4.0
lxml==6.1.3
matplotlib==3.11.2
matplotlib-inline==0.2.2
msgpack==1.2.2
multitasking==0.0.13
narwhals==2.26.0
numpy==2.4.6
packaging==26.3
pandas==2.3.3
parso==0.8.7
patsy==1.0.3
peewee==3.17.3
pexpect==4.9.0
pillow==12.3.0
platformdirs==4.11.10
pluggy==1.6.0
prompt_toolkit==3.0.53
protobuf==7.36.2
psutil==7.2.2
ptyprocess==0.7.0
pure_eval==0.2.4
purgedcv==0.1.6
pyarrow==25.0.1
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pyluach==2.3.0
pyparsing==3.3.2
pytest==9.1.1
python-dateutil==2.9.0.post0
pytz==2026.3.post1
PyYAML==6.0.3
quantstats==0.0.81
requests==2.34.2
scikit-learn==1.9.1
scipy==1.17.1
seaborn==0.13.2
six==1.17.0
soupsieve==2.9.2
sseclient-py==1.9.0
stack-data==0.6.3
statsmodels==0.15.0
tabulate==0.10.0
threadpoolctl==3.7.0
toolz==1.1.0
traitlets==5.16.1
typing-inspection==0.4.4
typing_extensions==4.16.0
tzdata==2026.4
urllib3==2.8.0
wcwidth==0.8.4
websockets==17.1
wrapt==2.4.1
yfinance==1.7.0
